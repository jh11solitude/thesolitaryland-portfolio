# ═══════════════════════════════════════════════════════════════
# RENDER ENVIRONMENT VARIABLES
# Set ALL of these in Render Dashboard → Your Service → Environment
# Never put real values in this file — it's documentation only
# ═══════════════════════════════════════════════════════════════

# ── Django core ────────────────────────────────────────────────

# Which settings file to use
DJANGO_SETTINGS_MODULE=config.settings.production

# Generate a NEW secret key for production — never reuse your dev key
# Run: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-production-secret-key-here

# Your Render subdomain + any custom domain
# Comma-separated, no spaces
ALLOWED_HOSTS=thesolitaryland.onrender.com,thesolitaryland.com

# ── Database ───────────────────────────────────────────────────

# Paste the Internal Database URL from your Render PostgreSQL instance
DATABASE_URL=postgres://user:password@host:5432/dbname

# ── Email (Gmail example) ──────────────────────────────────────

# Use Gmail with an App Password (not your main password)
# Google Account → Security → 2FA enabled → App Passwords → Generate
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=yourname@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password

# Where contact form submissions are emailed
CONTACT_EMAIL=jerome@jeromekoh.com

# ── CORS ──────────────────────────────────────────────────────

# Comma-separated list of allowed frontend origins
CORS_ALLOWED_ORIGINS=https://https://thesolitaryland.com,https://www.thesolitaryland.com

# ── Python ────────────────────────────────────────────────────

# Tells Python not to buffer stdout — logs appear in real time
PYTHONUNBUFFERED=1
